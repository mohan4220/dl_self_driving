# Install torch FIRST, separately, or pip pulls ~2.5 GB of CUDA wheels this
# project cannot use. It is deliberately not listed here for that reason:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# On Colab, torch+CUDA is preinstalled -- skip that first line, and swap
# onnxruntime for onnxruntime-gpu (see docs/COLAB.md).

# --- inference runtimes ---
# GPU MACHINES: replace this with onnxruntime-gpu, and install ONLY ONE of them.
# Both wheels ship the same onnxruntime/capi/*.so, so having both installed means
# the second silently overwrites the first while pip still lists both. The GPU
# build is a superset -- it provides CPUExecutionProvider too -- so nothing is
# lost by using it everywhere a GPU exists. See docs/COLAB.md.
onnxruntime==1.29.0
onnx
ultralytics==8.4.128
lap==0.5.13          # ByteTrack's linear-assignment solver; ultralytics auto-installs it on first .track() call

# --- perception ---
transformers         # CLIP ViT-B/32, zero-shot weather classification
pillow               # CLIP image preprocessing
rapidocr             # PP-OCR via ONNX; speed-limit digits. NOTE: package is `rapidocr` (v3), not `rapidocr_onnxruntime`

# --- core ---
opencv-python        # NOT -headless: cv2.imshow is needed for --sim and the per-module demos
numpy
pyyaml               # config.yaml

# --- reporting / dev ---
matplotlib           # tools/figures.py
tqdm
pytest
onnxruntime-gpu