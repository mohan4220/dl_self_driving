# Shared dependencies. Do NOT install this file directly -- it deliberately
# omits the ONNX Runtime build, without which the segmenter cannot load.
#
# Install one of these instead:
#   pip install -r requirements-cpu.txt     # no GPU
#   pip install -r requirements-gpu.txt     # CUDA GPU (Colab, workstation)

# --- perception ---
ultralytics==8.4.128
lap==0.5.13          # ByteTrack's linear-assignment solver; ultralytics auto-installs it on first .track() call
onnx                 # only needed to inspect/requantize models, not for inference
transformers         # CLIP ViT-B/32, zero-shot weather classification
pillow               # CLIP image preprocessing
rapidocr             # PP-OCR via ONNX; speed-limit digits. NOTE: package is `rapidocr` (v3), not `rapidocr_onnxruntime`

# --- core ---
opencv-python        # NOT -headless: cv2.imshow is needed for --sim and the per-module demos
numpy
pyyaml               # config.yaml

# --- reporting / dev ---
matplotlib           # tools/figures.py
tqdm
pytest
